// Temporarily pretty useless.
function Tile(name, id, char, description) {
    this.name = name;
    this.id = id;
    this.char = char;
    this.description = description;

    tileList.push(this);
}